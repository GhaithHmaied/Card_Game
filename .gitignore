# Frontend dependencies & build (run npm install in frontend/)
frontend/node_modules/
frontend/.next/

# Backend dependencies (run npm install in backend/)
backend/node_modules/

# Local git history helper scripts (not part of the app)
scripts/
